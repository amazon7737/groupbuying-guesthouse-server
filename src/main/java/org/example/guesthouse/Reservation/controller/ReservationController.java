package org.example.guesthouse.Reservation.controller;

import lombok.RequiredArgsConstructor;
import org.example.guesthouse.Reservation.service.ReservationService;
import org.springframework.web.bind.annotation.RestController;

@RestController
@RequiredArgsConstructor
public class ReservationController {

    private final ReservationService reservationService;

    // 함께 예약하기 -> 게스트 방 선택

    // 사용자가 예약한 하우스 조회

    // 사용자가 하우스 예약하기

    // 사용자가 하우스 취소하기

    // 지역, 날짜, 적게 남은 예약 순서로 조회 가능




}
